# Project Worlds — Station vNext UI

# API base URL. Leave empty when using the Vite proxy (proxies /api → :8000).
# Set to your backend URL for non-proxy setups (e.g., http://192.168.2.76:8000).
VITE_API_URL=
